# Docker Compose — PostgreSQL container credentials
# Copy to .env and fill in real values for production.
# Defaults (pitchforge / pitchforge_dev) are used automatically in dev if this file is absent.

DB_USER=pitchforge
DB_PASSWORD=pitchforge_dev
DB_NAME=pitchforge
DB_PORT=5432
